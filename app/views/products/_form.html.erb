<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-group">
      <span class="input-group-addon"><i class="glyphicon glyphicon-tag"></i> Denominación </span>
      
      <%= form.text_field :name, id: :product_name, :class=>"form-control", :type=>"text", :placeholder=>"Denominación" %>
  </div>
  <div class="input-group">
    <span class="input-group-addon"><i class="glyphicon glyphicon-shopping-cart"></i> Precio </span>
    <%= form.text_field :price, id: :product_name, :class=>"form-control", :type=>"text", :placeholder=>"Precio" %>
  </div>
  <div class="panel">
  </div>
  <div class="container-fluid">
    <div class="row">
        <div class="col-sm-11">
        </div>
        <div class="col-sm-1">
          <div class="btn-group">
            <div class="actions" >
              <%= form.submit :Aceptar, class: 'btn btn-success'%>
            </div>  
          </div>
        </div>
    </div>
  </div>
<% end %>
